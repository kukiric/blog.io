<div class="d-flex justify-content-between align-items-center">
    <div>
        <h1>{{ pageName }}</h1>
    </div>
    {{#if user }}
        <button class="btn btn-outline-success" onclick="newPost()">Criar novo post</button>
    {{/if}}
</div>
{{#if nextPage }}
    <span>Página {{ nextPage }}</span>
{{/if}}