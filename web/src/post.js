import $ from "jquery";

$(document).ready(() => {
    // Ativa os elementos do modo administrador
    if (window.username) {
        // Para cada post
        $("article.post").each((index, element) => {});
    }
});